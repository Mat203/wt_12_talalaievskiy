import React from 'react';
import Button from './Button';

const Task = () => {
    return (
        <div className="task-container">
            {}
            <Button />
        </div>
    );
};

export default Task;